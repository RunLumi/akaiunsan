{
  "explicitAny": 47,
  "measuredAt": "2026-09-09",
  "note": "Ratchet cap for explicit `any` occurrences in production src (excludes __tests__/test-utils), counted by scripts/count-explicit-any.mjs. Progression: 580 (baseline) -> 201 (typed selectors/props/ApiItem sweeps) -> 116 -> 90 -> 47 (plan DoD <50 met). Remaining counts are deliberate API/SDK boundaries (apiSlice response types, RTK interop casts, RN-navigation internals). Never raise; lower further in the same commit as each type-safety win."
}
